<section class="hero">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <div class="content">
          <h1><%= t("#{@product.slug}.product.main_header") %></h1>
          <h3><%= t("#{@product.slug}.product.sub_header") %></h3>
          <%= render 'product/cta' %>
        </div>
      </div>
    </div>
  </div>
</section>
