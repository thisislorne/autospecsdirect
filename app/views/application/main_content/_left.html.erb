<h1 class="main-header">Our Top Picks For Whey Protein</h1>
<p class="top-description">Looking for an excellent whey protein powder that gives you results fast, tastes great and isn't filled with artificial additives and fillers? We've compiled a list of the best proteins in the business which you can view below! Click on the products that interest you to find out more about how they can help you with building muscle, recovering after your workouts, and giving you the protein supplementation that you require!</p>
<table id="mainTable">
  <tbody>
    <% @products.each_with_index do |product, index| %>
    <tr onclick="javascript:location.href='<%= product_path("#{product.id}") %>'">
      <th><%= index + 1 %></th>
      <td>
        <div class="product-image">
          <%= image_tag product.image %>
        </div>
        <span class="title">
          <%= product.title %><br>
          <% points = @points.where(product: product.id) %>
          <% if points.present? %>
            <ul class="points">
            <% points.each do |point| %>
              <li><%= point.point %></li>
            <% end %>
            </ul>
          <% end %>
          <%= link_to 'See More...', product_path(product.id) %>
        </span>
      </td>
      <td>
        <%= image_tag product.brand_image %>
        <p><%= product.brand.upcase %></p>
      </td>
      <td>
        <%= image_tag 'star.png' %>
        <span class="number"><%= product.rating.to_f.round %><span class="grey">/5</span></span>
        <p><%= @reviews.where(product: product.id).count %> Reviews</p>
        <%= link_to 'View Now', product_path(product.id), class: 'view-now', target: '_blank' %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
